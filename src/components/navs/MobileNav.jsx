const MobileNav = () => {
  return (
    <nav className="mobile-nav">
      <button className="hamburger">☰</button>
      {/* Modal ili dropdown meni može ići ovde */}
    </nav>
  );
};

export default MobileNav;